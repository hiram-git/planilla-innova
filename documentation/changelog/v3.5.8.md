# v3.5.8 — 2025-11-18

Tipo: Feature/Improvement — Multitenancy scaffolding, vacations filters, attendance tolerances, PDF refinements

Componentes
- Multitenancy (DB por tenant):
  - config/master_database.php y App\Core\MasterDatabase para la conexión master.
  - Migración `tenants` (database/migrations/master) para almacenar conexiones por empresa.
  - App\Models\WizardModel con provisión de tenant: generar licencia, crear DB, placeholders para migraciones/seed, cifrado de credenciales.
  - Rutas wizard en App\Core\App y controlador App\Controllers\WizardController con namespace.
  - Validación de distribuidor remota por cURL, parámetros desde .env (DISTRIBUTOR_VALIDATION_URL, timeouts, SSL verify).
- Vacaciones:
  - Filtrado por tipo_planilla_id en /panel/vacation (sessionStorage + evento navbar).
  - Descripción de planillas de vacaciones en MAYUSCULAS.
  - PDF: horizontal, labels en español (Tipo/Estado), eliminado "Dias Habiles", mejor alineación, "Resumen de Dias" igual a Balance Actual.
- Asistencias:
  - Tolerancias de horario y almuerzo integradas en cálculo de horas trabajadas.
  - Clamp de horas nocturnas para turnos diurnos dentro del rango de tolerancia.
- Estabilidad UI:
  - Fallback jQuery y guards para evitar `$ is not defined` en vistas.

Notas
- No se almacenan secretos en código; endpoints y flags se leen desde .env.
- Pendientes: TenantResolver + DatabaseManager.forTenant + middleware; importTenantSchema real; CSRF en process-day; preservar tipo_planilla_id en enlaces internos.
